version: 2
jobs:
  build:
    docker:
      - image: abronan/rust-circleci:latest
    steps:
      - checkout
      - run: echo "hello world"
  test:
    docker:
      - image: abronan/rust-circleci:latest
    steps:
      - checkout
      - run: echo "hello world"
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
