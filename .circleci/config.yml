version: 2
jobs:
  test:
    docker:
      - image: circleci/node:8.11.3-browsers
    steps:
      - checkout
      - run: echo "hello world"
workflows:
  version: 2
  build_and_test:
    jobs:
      - test

